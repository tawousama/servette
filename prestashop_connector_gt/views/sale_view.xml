<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data>
        <record id="view_order_form_inherited" model="ir.ui.view">
            <field name="name">sale.order.form</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_shipping_id']" position="after">
                    <field name="presta_order_ref"/>
                    <!--                     <field name="to_be_exported" readonly="0"/>-->
                    <field name="prestashop_order" invisible="1"/>
                    <field name="prestaa_instance_id" invisible="1"/>
                    <field name="prestashops_order"/>


                    <field name="shop_id"/>
                    <!--<field name="shop_ids" readonly="1"/>-->
                    <field name="token" invisible="1"/>
                </xpath>

                <xpath expr="//field[@name='price_unit']" position="after">
                    <field name='gift'/>
                    <field name='gift_message' attrs="{'invisible':[('gift','!=',True)]}"/>
                    <field name='wrapping_cost' attrs="{'invisible':[('gift','!=',True)]}"/>
                </xpath>
                <xpath expr="//page[2]" position="after">
                    <page string="Prestashop Shipping Information">
                        <form string="Shipping">
                            <group col="2">
                                <field name='order_status'/>
                                <field name='order_status_update'/>
                                <separator colspan="2" string="Shipping Details"/>
                                <!--<field name="total_wght_product"/>-->
                                <field name="carrier_prestashop" required="0"/>
                                <separator colspan="2" string="Payment Details"/>
                                <field name="pretsa_payment_mode"/>
                            </group>
                        </form>
                    </page>
                    <page string="Prestashop Messages">
                        <group name="messages">
                            <field name="message_order_ids">
                                <tree>
                                    <field name="customer_id"/>
                                    <field name="employee_id"/>
                                    <field name="status"/>
                                    <field name="email" invisible="1"/>
                                    <field name="new_id" invisible="1"/>
                                    <field name="msg_prest_id" invisible="1"/>
                                    <field name="token" invisible="1"/>
                                    <field name="message"/>
                                    <button name="upload_customer_message" string="Send Message" type="object"
                                            attrs="{'invisible':[('msg_prest_id','!=',0)]}"/>
                                </tree>
                                <form>
                                    <group>
                                        <field name="customer_id"/>
                                        <field name="employee_id"/>
                                        <field name="status"/>
                                        <field name="email" invisible="1"/>
                                        <field name="new_id" invisible="1"/>
                                        <field name="msg_prest_id" invisible="1"/>
                                        <field name="token" invisible="1"/>
                                        <field name="message"/>
                                    </group>
                                </form>
                            </field>
                        </group>
                    </page>
                </xpath>
            </field>
        </record>

        <!--Manish new done group by shop working-->
        <!-- by me<record id="view_order_filter_inherited" model="ir.ui.view">
            <field name="name">sale.order.filter</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_sales_order_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//group/filter[3]" position="after">
                    <filter string="Shop" domain="[]" context="{'group_by':'shop_id'}"/>
                </xpath>
            </field>
        </record> -->

        <record id="inherit_action_quotations" model="ir.actions.act_window">
            <field name="name">Quotations</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order</field>
            <!--             <field name="view_type">form</field>
             -->
            <field name="view_id" ref="sale.view_quotation_tree"/>
            <field name="view_mode">tree,kanban,form,calendar,pivot,graph</field>
            <field name="context">{}</field>
            <field name="domain">[('state','in',('draft','sent','cancel')),('presta_id', '!=', False)]</field>
            <field name="search_view_id" ref="sale.view_sales_order_filter"/>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Create a Quotation, the first step of a new sale.
                </p>
                <p>
                    Your next actions should flow efficiently: confirm the Quotation
                    to a Sale Order, then create the Invoice and collect the Payment.
                </p>
                <p>
                    Note that once a Quotation becomes a Sale Order, it will be moved
                    from the Quotations list to the Sales Order list.
                </p>
            </field>
        </record>

        <record id="inherit_action_orders" model="ir.actions.act_window">
            <field name="name">Sales Orders</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order</field>
            <!--             <field name="view_type">form</field>
             -->
            <field name="view_mode">tree,kanban,form,calendar,pivot,graph</field>
            <field name="search_view_id" ref="sale.view_sales_order_filter"/>
            <field name="context">{'show_sale': True,'default_to_be_exported' : True}</field>

            <field name="domain">['|',('to_be_exported', '=', True),('state', 'not in', ('draft', 'sent',
                'cancel')),('presta_id', '!=', False)]
            </field>
        </record>


        <record id="inherit_product_template_action" model="ir.actions.act_window">
            <field name="name">Products</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">product.template</field>
            <field name="view_mode">kanban,tree,form</field>
            <!--             <field name="view_type">form</field>
             -->
            <field name="domain">['|',('presta_id', '!=', False),('product_to_be_exported', '=', True)]</field>
            <!--<field name="view_id" ref="product.product_template_kanban_view"/>-->
            <field name="context">{"search_default_filter_to_sell":1,'default_product_to_be_exported': True}</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to define a new product.
                </p>
                <p>
                    You must define a product for everything you sell, whether it's a physical product, a consumable or
                    a service you offer to customers.
                </p>
                <p>
                    The product form contains information to simplify the sale process: price, notes in the quotation,
                    accounting data, procurement methods, etc.
                </p>
            </field>
        </record>


        <record id="inherit_action_partner_form" model="ir.actions.act_window">
            <field name="name">Customers</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <!--             <field name="view_type">form</field>
             -->
            <field name="view_mode">kanban,tree,form</field>
            <field name="context">{"search_default_customer":1}</field>
            <field name="domain">[('prestashop_customer','=',True)]</field>
            <field name="search_view_id" ref="base.view_res_partner_filter"/>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to add a contact in your address book.
                </p>
                <p>
                    Odoo helps you easily track all activities related to
                    a customer; discussions, history of business opportunities,
                    documents, etc.
                </p>
            </field>
        </record>


        <record id="view_presta_order_status_form" model="ir.ui.view">
            <field name="name">presta.order.status.form</field>
            <field name="model">presta.order.status</field>
            <field name="arch" type="xml">
                <form string="Order Status">
                    <group>
                        <field name="name" readonly="1"/>
                        <field name="presta_id" readonly="1"/>
                        <field name="confirm_order"/>
                        <field name="paid"/>
                        <field name="shipped"/>
                        <!--                        <button name="update" type="object" string="update"/>-->
                        <!--                        <field name="paid"/>-->
                    </group>
                </form>
            </field>
        </record>

        <record id="view_presta_order_status_tree" model="ir.ui.view">
            <field name="name">presta.order.status.tree</field>
            <field name="model">presta.order.status</field>
            <field name="arch" type="xml">
                <tree string="Order Status">
                    <field name="name"/>
                    <field name="presta_id"/>
                    <field name="confirm_order"/>
                    <field name="paid"/>
                    <field name="shipped"/>
                </tree>
            </field>
        </record>

        <record id="action_presta_order_status" model="ir.actions.act_window">
            <field name="name">Order Status</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">presta.order.status</field>
            <field name="view_mode">tree,form</field>
        </record>

        <record id="update_orders_status_view_action" model="ir.actions.server">
            <field name="name">Update Order Status</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="binding_model_id" ref="sale.model_sale_order"/>
            <field name="binding_view_types">tree,form</field>
            <field name="state">code</field>
            <field name="code">action = records.action_update_order_status()</field>
        </record>

        <record id="sale.view_order_product_search" model="ir.ui.view">
        <field name="name">sale.report.search</field>
        <field name="model">sale.report</field>
        <field name="arch" type="xml">
            <search string="Sales Analysis">
                <field name="date"/>
                <filter string="Date" name="year" invisible="1" date="date" default_period="this_year"/>
                <filter string="Quotations" name="Quotations" domain="[('state','in', ('draft', 'sent'))]"/>
                <filter string="Sales Orders" name="Sales" domain="[('state','not in',('draft', 'cancel', 'sent'))]"/>
                <separator/>
                <filter name="filter_date" date="date" default_period="this_month"/>
                <filter name="filter_order_date" invisible="1" string="Order Date: Last 365 Days" domain="[('date', '&gt;=', (datetime.datetime.combine(context_today() + relativedelta(days=-365), datetime.time(0,0,0))).strftime('%Y-%m-%d %H:%M:%S'))]"/>
                <separator/>
                <field name="user_id"/>
                <field name="team_id"/>
                <field name="product_id"/>
                <field name="categ_id"/>
                <field name="partner_id"/>
                <field name="country_id"/>
                <field name="industry_id"/>
                <group expand="0" string="Extended Filters">
                    <field name="categ_id" filter_domain="[('categ_id', 'child_of', self)]"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                </group>
                <group expand="1" string="Group By">
                    <filter string="Salesperson" name="User" context="{'group_by':'user_id'}"/>
                    <filter string="Sales Team" name="sales_channel" context="{'group_by':'team_id'}"/>
                    <filter string="Customer" name="Customer" context="{'group_by':'partner_id'}"/>
                    <filter string="Customer Country" name="country_id" context="{'group_by':'country_id'}"/>
                    <filter string="Customer Industry" name="industry_id" context="{'group_by':'industry_id'}"/>
                    <filter string="Product" name="Product" context="{'group_by':'product_id'}"/>
                    <filter string="Product Category" name="Category" context="{'group_by':'categ_id'}"/>
                    <filter string="Status" name="status" context="{'group_by':'state'}"/>
                    <filter string="Company" name="company" groups="base.group_multi_company" context="{'group_by':'company_id'}"/>
                    <separator/>
                    <filter string="Order Date" name="date" context="{'group_by':'date'}"
                            invisible="context.get('sale_report_view_hide_date')"/>
                    <filter string="Order Date" name="group_by_date_day" context="{'group_by':'date:day'}"
                            invisible="not context.get('sale_report_view_hide_date')"/>
                </group>
            </search>
        </field>
    </record>

    </data>
</odoo>
